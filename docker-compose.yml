services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=sqlite:///./shiftmed.db
    restart: always

  # Frontend placeholder (to be built later)
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
